apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: fitverse
  labels:
    app: mysql
spec:
  # THIS LINE MAKES IT HEADLESS
  clusterIP: None

  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP