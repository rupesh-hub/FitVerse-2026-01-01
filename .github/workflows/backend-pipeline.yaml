name: backend pipeline

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - 'docker/backend/Dockerfile'
      - '.github/workflows/backend-*.yaml'

# Prevents conflicts if frontend and backend pipelines update manifests simultaneously
concurrency:
  group: gitops-updates
  cancel-in-progress: false

jobs:
  compile-test-build:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'corretto'
          cache: 'maven' # Speeds up build by caching dependencies

      - name: Build with Maven
        run: |
          cd backend
          ./mvnw clean package -DskipTests

  docker:
    name: Build & Push Docker
    runs-on: ubuntu-latest
    needs: [ compile-test-build ]
    outputs:
      VERSION: ${{ steps.extract_version.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract version
        id: extract_version
        run: |
          cd backend
          # Extract version from pom.xml without running full maven
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_SECRET_KEY }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: backend
          file: docker/backend/Dockerfile
          push: true
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/fitverse-backend:${{ steps.extract_version.outputs.VERSION }}
            ${{ secrets.DOCKERHUB_USERNAME }}/fitverse-backend:latest
          build-args: |
            PROJECT_VERSION=${{ steps.extract_version.outputs.VERSION }}
            ACTIVE_PROFILE=production

  update-deployments:
    name: Update Manifests
    runs-on: ubuntu-latest
    needs: [ docker ]
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.TOKEN }}

      - name: Setup Git Config
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Update Manifest Files
        env:
          VERSION: ${{ needs.docker.outputs.VERSION }}
          USER: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          PROD_COMPOSE_PATH="docker/docker-compose/production/docker-compose.yaml"
          DOCKER_COMPOSE_PATH="docker/docker-compose/docker/docker-compose.yaml"
          DEFAULT_COMPOSE_PATH="docker/docker-compose/default/docker-compose.yaml"
          K8S_PATH="kubernetes/backend-deployment.yaml"
          IMAGE_NAME="$USER/fitverse-backend:$VERSION"

          # 1. Update Docker Compose using yq (Targeting the backend service specifically)
          yq -i ".services.fitverse-backend.image = \"$IMAGE_NAME\"" "$PROD_COMPOSE_PATH"
          yq -i ".services.fitverse-backend.image = \"$IMAGE_NAME\"" "$DOCKER_COMPOSE_PATH"
          yq -i ".services.fitverse-backend.image = \"$IMAGE_NAME\"" "$DEFAULT_COMPOSE_PATH"

          # 2. Update Kubernetes (Targeting the backend container name)
          yq -i "(.spec.template.spec.containers[] | select(.name == \"fitverse-backend\") | .image) = \"$IMAGE_NAME\"" "$K8S_PATH"

          # Store variables for the next step 
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Commit and Push
        run: |
          git add .
          if git diff --staged --quiet; then
            echo "No changes to manifests."
          else
            git commit -m "chore(backend): update image tag to ${{ env.VERSION }} [skip ci]"
            git pull --rebase origin main
            git push origin main
          fi